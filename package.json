{
  "name": "pvi-samples",
  "private": true,
  "packageManager": "yarn@4.3.1",
  "scripts": {
    "dialog": "yarn workspace @project/dialog",
    "mock": "yarn workspace mock",
    "initialize-db": "yarn mock initialize",
    "lib:fluid": "yarn workspace fluid"
  },
  "workspaces": [
    "library/**/*",
    "project/**/*",
    "mock"
  ],
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "typescript": "^5.4.3"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
